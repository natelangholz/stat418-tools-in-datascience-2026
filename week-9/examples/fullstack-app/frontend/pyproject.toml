[project]
name = "week9-fullstack-frontend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.30.0",
  "requests>=2.31.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]